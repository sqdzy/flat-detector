# flat-detector v0.2: verified offline MCP implementation archive

Build: 2026-09-23. Source repo used for planning: `sqdzy/flat-detector`, verified empty and left untouched in this delivery. This ZIP is source code, not a pre-deployed or separately signed binary.

Current local checks: **39 passing, 1 skipped** (the skipped test requires a real PostgreSQL server); Python coverage is recorded in `docs/IMPLEMENTATION_STATUS.md`. MCP SDK 2.2.0 is now truly installed and used in **two additional integration tests**, including four real Client tool calls against an isolated seeded database and HTTP transport Host validation (allowed 200 / unexpected 421). SQLite migration, synthetic Telegram scheduling/consent and transport mocks still pass. An additional regression test verifies stale `CLAIMED` sends are marked `UNCERTAIN` without automatic resend. The PostgreSQL migration/unique-digest/concurrent-send integration test is included in CI with an ephemeral PostgreSQL 17 service; it has not run here because there is no local server or Docker.

Dependency recovery: the owner restored the runner's rclone authorization. Existing `environment-collector` [run 35897661034](https://github.com/sqdzy/environment-collector/actions/runs/35897661034) was retried (attempt 2) and **succeeded**. Artifact `pip-wheelhouse-py3.13.5-linux-x86_64-46b939e8cdca3a71` has `state=complete`, archive SHA-256 `d61e262249a26e87d8746462ddbe157c9f6c97ac7b211165fedc6255e7cf1345`. The ZIP, record and checksums were downloaded via connected Google Drive; the outer hash and all inner SHA256SUMS matched. Python 3.13.5 clean offline venv installed both exact requested packages, and `pip check` returned no broken requirements. This wheelhouse covers MCP + psycopg, **not all application dependencies** needed to install the entire project offline from scratch.

No Telegram bot is connected or running, no sources are approved or fetched, no Docker service was deployed, no private MCP tunnel has been opened. `FD_DEMO_DELIVERY=false` is the default. The optional approved JSON feed transport has mocked tests only and requires an egress DNS/IP firewall before live access. No major real-estate portal scraper exists.

Security note: source authorization proof is a deliberate human assertion; the application cannot itself judge whether a real-world third-party license is genuine. Disable any source if permission scope changes or expires.
